[package]
name = "dungeon-card"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["wayland", "debug", "mesh_picking"] }
bevy-inspector-egui = "0.36.0"
enum-map = "2.7.3"
